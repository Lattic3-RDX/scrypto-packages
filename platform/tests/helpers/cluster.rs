// use super::platform::Ledger;
// use scrypto_test::prelude::*;

// #[derive(Debug, Clone)]
// pub struct ClusterComponent {
//     address: ComponentAddress,
// }

// impl ClusterComponent {
//     pub fn publish(ledger: &mut Ledger, path: &str) -> Self {
//         let component = ledger.compile_and_publish(path);

//         let cluster = ClusterComponent {
//             address: component,
//         }
//     }
// }
